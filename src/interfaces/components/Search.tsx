export interface SearchNameInputProps {
    searchName: string;
    onChange: (text: string) => void;
  }

export interface SearchSymbolInputProps {
    searchSymbol: string;
    onChange: (text: string) => void;
  }
  